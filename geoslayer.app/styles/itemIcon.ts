import { StyleSheet } from 'react-native';

export const itemIconStyles = StyleSheet.create({
  /** The emoji fallback, sized to match the image it stands in for. */
  emoji: {
    textAlign: 'center',
  },
});
