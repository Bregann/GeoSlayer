name: Build and Publish Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-publish:
    runs-on: self-hosted
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -f GeoSlayer/Dockerfile -t 192.168.1.100:5000/geoslayer:latest .

      - name: Push Docker image
        run: docker push 192.168.1.100:5000/geoslayer:latest
